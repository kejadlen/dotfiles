# ignore everything by default
*

!.gitignore
!CLAUDE.md
!settings.json
!skills
!statusline.sh

# Git won't look inside an ignored directory, so un-ignoring the
# directory and its contents are both required.
!hooks/
!hooks/**
!output-styles/
!output-styles/**
!rules/
!rules/**
